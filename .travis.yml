language: node_js
node_js:
  - 0.10
  - 6
  - 7
install:
  - npm install .
  - npm install -g mocha
after_success:
  - npm install -g nyc
  - npm install -g codecov
  - nyc --reporter lcov npm test && codecov
