{
  "name": "k-http",
  "version": "1.3.4",
  "description": "kinvey http",
  "main": "khttp.js",
  "engines": {
    "node": ">= 0.10"
  },
  "license": "Kinvey",
  "repository": "git+ssh://github.com/andrasq/node-k-http",
  "dependencies": {
  },
  "devDependencies": {
  },
  "scripts": {
    "test": "mocha --version && mocha -b test-khttp.js",
    "coverage": "nyc --version && nyc --include khttp.js --reporter lcov --reporter text mocha test-khttp.js || echo 'nyc not installed; try \"npm install -g nyc\"'",
    "clean": "rm -rf .nyc_output coverage",
    "coveralls": "nyc --reporter text --reporter text-lcov npm run test | coveralls"
  }
}
