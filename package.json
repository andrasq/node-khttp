{
  "name": "khttp",
  "version": "1.3.8-dev",
  "description": "fast easy Kinvey http",
  "main": "khttp.js",
  "engines": {
    "node": ">= 0.8"
  },
  "license": "Kinvey",
  "private": true,
  "repository": "git+ssh://github.com/andrasq/node-khttp",
  "keywords": [ "Kinvey", "http", "fast", "easy", "simple" ],
  "dependencies": {
  },
  "devDependencies": {
  },
  "scripts": {
    "test": "mocha -b test-khttp.js",
    "coverage": "nyc --include khttp.js --reporter lcov --reporter text mocha test-khttp.js",
    "clean": "rm -rf .nyc_output coverage"
  }
}
