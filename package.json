{
  "name": "k-http",
  "version": "1.3.6",
  "description": "kinvey http",
  "main": "khttp.js",
  "engines": {
    "node": ">= 0.10"
  },
  "license": "Kinvey",
  "repository": "git+ssh://github.com/andrasq/node-k-http",
  "dependencies": {
  },
  "devDependencies": {
  },
  "scripts": {
    "test": "mocha -b test-khttp.js",
    "coverage": "nyc --include khttp.js --reporter lcov --reporter text mocha test-khttp.js",
    "clean": "rm -rf .nyc_output coverage"
  }
}
